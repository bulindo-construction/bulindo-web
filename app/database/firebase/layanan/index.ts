import { firebase_storage } from "../config";
import { getDownloadURL } from "@firebase/storage";

export const LayananStorageApi = () => {
  const getAllLayananImages = async (): Promise<string[]> => {
    return []
  }
}